# [기능 지침서] llms.txt 자동 생성기

**작성일**: 2026-08-20
**대상 서비스**: nexalab.app 무료 도구 섹션
**연계 기능**: SEO·GEO 무료 점검 도구 (`nexalab_seo-geo-checker`)와 페어로 동작
**작성 목적**: SEO/GEO 체커에서 "llms.txt 없음" 진단을 받은 방문자가 이탈 없이 바로 해결까지 완료하도록 만드는 전환형 생성 도구

---

## 1. 한 줄 정의

"진단만 하지 말고, 그 자리에서 고쳐주자" — 사이트 정보 몇 개만 입력하면 AI 크롤러가 인식하기 쉬운 형식의 `llms.txt` 파일을 즉시 생성해서 다운로드/복사할 수 있는 도구예요.

SEO/GEO 체커의 "❌ llms.txt 없음" 항목 옆에 **[지금 만들기]** 버튼을 붙여서, 진단(Diagnose) → 해결(Fix) 흐름을 끊김 없이 이어주는 게 핵심이에요.

---

## 2. 왜 이 기능인가

- SEO/GEO 체커가 이미 llms.txt 부재를 "GEO 준비도 하락 요인"으로 지적하고 있음 → 별도 유입 없이 기존 트래픽을 그대로 전환 가능
- llms.txt는 아직 표준화 초기 단계라 "형식이 헷갈려서" 못 만드는 사람이 많음 → 생성기가 그 마찰을 제거
- 구현 난이도가 낮음(텍스트 템플릿 조합 수준) 대비 체감 가치가 커서 리소스 대비 ROI가 가장 좋은 후속 기능

---

## 3. 입력 필드 설계

| 필드명 | 타입 | 필수 여부 | 설명 / 플레이스홀더 예시 |
|---|---|---|---|
| 사이트명 | 텍스트 | 필수 | 예: "nexalab" |
| 사이트 URL | 텍스트 (URL 검증) | 필수 | 예: "https://nexalab.app" |
| 한 줄 요약 (사이트 설명) | 텍스트 (최대 200자) | 필수 | 예: "AI 애플리케이션 개발 블로그이자 실무 도구 모음" |
| 핵심 콘텐츠 카테고리 | 다중 선택 or 태그 입력 | 필수 | 예: "AI 개발 블로그글", "무료 유틸리티", "포트폴리오" |
| 주요 페이지 리스트 | 반복 입력 (제목 + URL + 한 줄 설명) | 선택 (최소 1개 권장) | 예: "SEO/GEO 체커 – /tools/seo-geo-check – URL 하나로 SEO·GEO 무료 진단" |
| 연락처/문의 링크 | 텍스트 | 선택 | 예: "https://nexalab.app/contact" |
| 마지막 업데이트일 | 날짜 (자동 오늘 날짜 기본값) | 자동 | 사용자가 수정 가능하게 열어둠 |

**UX 팁**:
- SEO/GEO 체커에서 넘어온 경우, URL과 사이트명은 체커가 이미 파싱한 값으로 **자동 프리필**해서 재입력 마찰을 없애야 해요 (쿼리 파라미터 또는 세션 스토리지로 전달).
- "주요 페이지 리스트"는 최대 10개까지, `+ 페이지 추가` 버튼으로 동적 추가.

---

## 4. 생성 로직 및 출력 형식

### 4-1. llms.txt 표준 포맷 기준

llms.txt는 마크다운 기반의 단순 텍스트 규격으로, 아래 구조를 따라야 해요:

```
# {사이트명}

> {한 줄 요약}

{선택: 사이트에 대한 추가 설명 1~2문단}

## {카테고리1}

- [{페이지 제목}]({URL}): {한 줄 설명}
- [{페이지 제목}]({URL}): {한 줄 설명}

## {카테고리2}

- [{페이지 제목}]({URL}): {한 줄 설명}

## Optional

- [{연락처/문의}]({URL})
```

### 4-2. 생성 예시 (nexalab 기준 샘플 데이터)

```
# nexalab

> AI 애플리케이션 개발 블로그이자 실무 도구 모음

nexalab은 AI 기반 애플리케이션 개발 과정을 시리즈로 기록하고,
실무에 바로 쓸 수 있는 무료 도구(견적서 생성기, 손익 계산기,
SEO/GEO 체커 등)를 제공하는 사이트입니다.

## AI 개발 블로그글

- [Harubite 개발기](/series/harubite): AI 기반 서비스 개발 과정 기록
- [Venus Gecko 개발기](/series/venus-gecko): AI 서비스 구축 시리즈

## 무료 유틸리티

- [AI 견적서 생성기](/tools/quote-generator): 프로젝트 정보 입력 시 견적서 자동 생성
- [손익 계산기](/tools/profit-calculator): 매출·비용 입력으로 손익 즉시 계산
- [SEO/GEO 무료 체커](/tools/seo-geo-check): URL 입력으로 SEO·GEO 준비도 동시 진단

## Optional

- [문의하기](/contact)
```

### 4-3. 처리 로직 요약

1. 사용자 입력값을 위 템플릿 변수에 매핑
2. 카테고리별로 페이지 리스트를 그룹핑해서 `##` 섹션으로 자동 분류
3. URL은 상대경로/절대경로 모두 허용하되, 사이트 URL 기준으로 정규화(normalize)
4. 특수문자·마크다운 문법 충돌 방지를 위한 이스케이프 처리 (`[`, `]`, `(`, `)` 등)

---

## 5. 결과 화면 UX

```
✅ llms.txt 생성 완료!

[ 미리보기 박스 - 생성된 텍스트 전체 표시, 코드블록 스타일 ]

[ 📋 복사하기 ]   [ ⬇️ 다운로드 (llms.txt) ]

📌 이 파일을 사이트 루트 경로(https://내도메인.com/llms.txt)에
   업로드하면 AI 검색엔진이 사이트 구조를 더 쉽게 파악할 수 있어요.

💡 SEO/GEO 체커로 다시 점검하기 →
```

- **복사하기**: 클립보드 API로 원클릭 복사
- **다운로드**: `llms.txt` 파일명으로 즉시 다운로드 (MIME type: `text/plain`)
- 하단 CTA로 SEO/GEO 체커 재방문 유도 → "우리 사이트에도 반영했더니 GEO 점수가 올랐다"는 순환 경험 설계
- 톤: 축하 이모지(✅)로 완료감을 주되, 설명 문구는 담백하게. 과장된 카피 지양

---

## 6. 기술 구현 방향

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js API Route 불필요 — **클라이언트 사이드에서 전량 처리 가능** (템플릿 문자열 조합 수준이라 서버 호출 없이 즉시 생성) |
| 상태 관리 | React useState로 폼 입력값 관리, 페이지 리스트는 배열 상태로 동적 추가/삭제 |
| SEO 체커 연동 | 체커 결과 페이지에서 쿼리 파라미터(`?site=` `&url=`)로 이 도구에 값 전달 → 진입 시 자동 프리필 |
| 파일 다운로드 | `Blob` + `URL.createObjectURL`로 클라이언트에서 바로 다운로드 처리 (서버 저장 불필요) |
| 캐싱/제한 | 서버 호출이 없으므로 캐싱·요청 제한 정책 불필요 (SEO 체커와 다른 지점) |

**참고**: SEO/GEO 체커 지침서와 달리 이 도구는 외부 fetch나 API 호출이 필요 없어서 구현이 훨씬 가벼워요. 기존 코드 구조와 충돌 시, 기존 폼 컴포넌트 스타일(견적서 생성기 등)을 그대로 재사용하는 걸 우선 권장해요.

---

## 7. 연계 지점 (기존 기능과의 연결)

| 연결 지점 | 방식 |
|---|---|
| SEO/GEO 체커 → llms.txt 생성기 | 체커 결과의 "❌ llms.txt 없음" 항목 옆 `[지금 만들기]` 버튼, 클릭 시 URL·사이트명 쿼리로 전달 |
| llms.txt 생성기 → SEO/GEO 체커 | 생성 완료 화면 하단 "다시 점검하기" 링크 |
| 배너 지침서(`nexalab_banner_지침서.md`)와의 톤 정합성 | Option B 톤(문제 제기 → 해결 → 액션 CTA) 유지: "llms.txt가 없어서 AI가 못 읽고 있어요 → 지금 만들면 끝나요 → 만들기" |

---

## 8. 다음 액션 아이템

- [ ] SEO/GEO 체커 결과 화면에 `[지금 만들기]` CTA 버튼 추가 (쿼리 파라미터 설계 포함)
- [ ] 입력 폼 컴포넌트 구현 (기존 손익계산기 폼 스타일 재사용 검토)
- [ ] 템플릿 문자열 생성 로직 구현 및 특수문자 이스케이프 테스트
- [ ] 복사하기/다운로드 버튼 동작 구현
- [ ] 결과 화면에서 체커로 되돌아가는 CTA 배치
