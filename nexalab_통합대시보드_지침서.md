# [기능 지침서] 통합 대시보드 (내 사업 건강검진표)

**작성일**: 2026-08-20
**대상 서비스**: nexalab.app PWA
**연계 기능**: AI 견적서 생성기, 손익 계산기, SEO/GEO 무료 체커 (기존 3종 도구 통합)
**작성 목적**: 개별 도구 사용 후 흩어지던 결과를 한 화면에 모아, 재방문·재사용을 유도하는 "허브" 화면 구축

---

## 1. 한 줄 정의

"따로 쓰던 도구들을, 내 사업 현황판 하나로" — 견적서·손익계산기·SEO/GEO체커의 최근 결과를 홈 화면 하나에 모아 보여주고, PWA 특성을 살려 "홈 화면에 추가"했을 때 진짜 앱처럼 느껴지게 만드는 대시보드예요.

신규 기능 개발이 아니라 **기존 3개 도구의 결과 데이터를 재구성**하는 작업이라, 개발 리소스 대비 체감 가치가 커요.

---

## 2. 왜 이 기능인가

- 현재는 도구 3개가 각각 독립적으로 존재 → 사용자가 견적서 뽑고 나가면 손익계산기·SEO체커 존재 자체를 모를 수 있음
- PWA로 "홈 화면에 추가"까지 구현했는데, 진입 시 첫 화면이 블로그 글 목록이면 "앱처럼" 느껴지지 않음 → 대시보드가 PWA 진입점이 되어야 리텐션 효과가 커짐
- "지난달 대비 변화"처럼 **히스토리 기반 트렌드**는 개별 도구 페이지에선 보여줄 수 없고, 통합 대시보드에서만 가능한 가치

---

## 3. 정보 구조(IA) 설계

### 3-1. 대시보드 진입 방식

| 진입 경로 | 동작 |
|---|---|
| PWA 홈 화면 아이콘 실행 | 대시보드를 시작 화면(start_url)으로 설정 |
| 웹 브라우저 직접 방문 (`nexalab.app/dashboard`) | 동일 대시보드 노출 |
| 기존 블로그 상단 네비게이션 | "대시보드" 메뉴 추가 |

### 3-2. 화면 구성 (섹션별)

```
┌─────────────────────────────────────┐
│  👋 안녕하세요! (또는 오늘의 요약)      │
├─────────────────────────────────────┤
│  📊 내 사업 건강검진표                  │
│  ┌───────┐ ┌───────┐ ┌───────┐      │
│  │견적서  │ │손익계산│ │SEO/GEO│      │
│  │최근생성│ │최근결과│ │최근점수│      │
│  └───────┘ └───────┘ └───────┘      │
├─────────────────────────────────────┤
│  📈 트렌드 (결과 히스토리가 있을 때만)   │
│  "SEO 점수 지난달 대비 +12점 ↑"        │
├─────────────────────────────────────┤
│  🔗 도구 간 추천 (교차 유도)            │
│  "GEO 점수가 낮아요 → llms.txt 만들기"  │
├─────────────────────────────────────┤
│  📚 최근 블로그 시리즈 글 (선택적 노출)  │
└─────────────────────────────────────┘
```

### 3-3. 카드별 상세 명세

| 카드 | 데이터 없을 때 (Empty State) | 데이터 있을 때 |
|---|---|---|
| 견적서 생성기 | "아직 만든 견적서가 없어요 → [지금 만들기]" | 최근 생성일, 프로젝트명, 견적 금액 요약 + [다시 만들기] |
| 손익 계산기 | "손익 계산 결과가 없어요 → [지금 계산하기]" | 최근 계산일, 순이익 요약 + [다시 계산하기] |
| SEO/GEO 체커 | "아직 점검한 사이트가 없어요 → [지금 점검하기]" | 최근 점검 URL, SEO/GEO 점수, 등급(A~D) + [다시 점검하기] |

**중요**: 세 도구 모두 기본적으로 **로그인 없이 사용 가능**한 구조이므로, 히스토리 저장은 아래 4번 항목에서 다루는 저장 방식 선택에 따라 Empty State가 기본값이 될 수 있음을 감안해서 설계해야 해요.

---

## 4. 데이터 저장 방식 (핵심 의사결정 필요 지점)

로그인 시스템 유무에 따라 두 가지 옵션이 있어요. 현재 nexalab.app에 별도 회원 시스템이 없다면, **옵션 A(로컬 우선)**로 시작하고 이메일 게이트(리드 매그넷 전략)와 연결해 옵션 B로 확장하는 걸 추천해요.

| 옵션 | 저장 방식 | 장점 | 단점 |
| ---|---|---|---|
| **A. 로컬 스토리지 기반** (1단계 추천) | 브라우저 `localStorage`에 최근 결과 저장 (도구별 최근 3~5건) | 회원가입 불필요, 즉시 구현 가능, 개인정보 이슈 없음 | 기기 변경/브라우저 초기화 시 데이터 소실 |
| **B. 이메일 연동 저장** (2단계 확장) | 결과 생성 시 이메일 입력 유도 → 서버에 결과 매핑 저장 | 기기 간 동기화, 리드 매그넷과 자연 결합 | 회원 시스템/DB 설계 필요, 이메일 게이트가 도구 사용 마찰을 높일 수 있음 |

**권장 로드맵**: 우선 옵션 A로 대시보드를 띄우고, "결과를 이메일로 저장하고 다음에도 보기" 버튼을 옵션으로 추가해서 B로 자연스럽게 넘어가는 구조가 안전해요.

---

## 5. 도구 간 자동 연결 CTA 설계

기존 도구 결과를 바탕으로 "다음 행동"을 제안하는 규칙 기반 추천 로직이에요. 복잡한 알고리즘이 아니라 조건문 수준으로 충분해요.

| 조건 | 추천 CTA | 연결 대상 |
|---|---|---|
| GEO 점수 < 60점 & llms.txt 미생성 | "GEO 점수를 높이려면 llms.txt부터 만들어보세요" | llms.txt 생성기 (신규 기능) |
| 손익계산기 결과 순이익이 음수 | "비용 구조를 다시 점검해보고 싶다면 견적서도 함께 확인해보세요" | 견적서 생성기 |
| 견적서 생성 후 7일 이상 재방문 없음 (로컬 타임스탬프 기준) | "그 프로젝트, 손익은 어떻게 되고 있나요?" | 손익 계산기 |
| SEO 점수만 낮고 GEO는 양호 | "메타데이터·구조화 데이터부터 손봐보세요" | 블로그 관련 시리즈 글로 연결 |

**톤 가이드**: 배너 지침서의 Option B 톤(문제 제기 → 해결 제안 → 액션 CTA)을 그대로 따라가면 대시보드 전체의 카피 톤이 일관돼요.

---

## 6. PWA 연동 시 고려사항

| 항목 | 설정 방향 |
|---|---|
| `start_url` | `manifest.json`의 `start_url`을 대시보드 경로(`/dashboard`)로 지정할지, 기존 블로그 홈으로 유지할지 결정 필요 → **대시보드로 변경 추천** (재방문자에게 더 유용한 진입점) |
| 오프라인 대응 | 최근 저장된 로컬 결과는 오프라인에서도 조회 가능하도록 서비스워커 캐싱 전략에 포함 (단, 도구 재실행은 온라인 필요) |
| 홈 화면 아이콘/스플래시 | 대시보드가 진입점이 되므로, 스플래시 화면에 "내 사업 건강검진표" 컨셉을 살짝 반영하면 좋음 |
| 알림(Push) 확장 여지 | 지금 단계는 아니지만, 향후 "이번 달 SEO 점수 리포트 도착" 같은 푸시 알림으로 확장 가능한 구조를 염두에 두고 데이터 스키마 설계 |

---

## 7. 기술 구현 방향

| 항목 | 내용 |
|---|---|
| 데이터 소스 | 각 도구가 생성 시점에 `localStorage`에 결과 요약(JSON)을 저장하도록 각 도구 코드에 최소한의 훅 추가 필요 |
| 저장 키 설계(예시) | `nexalab_quote_history`, `nexalab_profit_history`, `nexalab_seo_history` — 각 배열 형태로 최근 N건 유지 |
| 대시보드 페이지 | Next.js 페이지 1개 신규 생성 (`/dashboard`), 클라이언트 컴포넌트로 `localStorage` 읽어서 카드 렌더링 |
| 빈 상태 처리 | 세 도구 모두 미사용 시 온보딩형 안내 화면으로 전환 (각 도구 최초 사용 유도) |
| 기존 코드 충돌 시 | 세 도구의 결과 저장 로직이 없다면, 각 도구의 "결과 표시" 단계 완료 시점에 저장 훅을 추가하는 것부터 선행 작업으로 필요함 — 이 부분은 기존 코드 구조 확인 후 별도 작업 항목으로 분리 권장 |

---

## 8. 다음 액션 아이템

- [ ] 데이터 저장 방식 확정 (옵션 A 로컬 스토리지로 1단계 시작 여부 확인)
- [ ] 기존 3개 도구에 결과 저장 훅 추가 (각 도구 "결과 완료" 시점 파악 필요)
- [ ] `/dashboard` 페이지 와이어프레임 확정 (카드 3개 + 트렌드 + 추천 CTA 섹션)
- [ ] 도구 간 자동 연결 CTA 규칙 최종 확정 (5번 표 기준 우선순위 조정)
- [ ] `manifest.json`의 `start_url` 변경 여부 결정
- [ ] Empty State 카피 작성 (배너 지침서 톤 가이드 적용)
